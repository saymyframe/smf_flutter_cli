name: smf_contracts
description: Contracts, DSL, and shared codegen interfaces used across SMF modules (routing, DI, code generation).
version: 0.2.0
homepage: https://saymyframe.com
repository: https://github.com/saymyframe/smf_flutter_cli/tree/main/packages/smf_contracts
issue_tracker: https://github.com/saymyframe/smf_flutter_cli/issues
topics: [dsl, codegen, routing, dependency-injection, interfaces]
license: Apache-2.0

environment:
  sdk: ">=3.6.0 <4.0.0"

resolution: workspace

dependencies:
  mason: ^0.1.1
  meta: ^1.17.0
  smf_contribution_engine: ^0.2.0

dev_dependencies:
  analyzer: ^8.1.0
  test: ^1.24.0
  very_good_analysis: ^10.3.0
