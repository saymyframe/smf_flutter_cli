name: fake_di
description: A fake DI container for the tests of the SMF pipeline. Not a module to use.
version: 0.0.0
publish_to: none

environment:
  sdk: ">=3.6.0 <4.0.0"

resolution: workspace

dependencies:
  mason: ^0.1.1
  smf_contracts: ^0.2.0

dev_dependencies:
  very_good_analysis: ^10.3.0
