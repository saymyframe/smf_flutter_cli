name: fixture_registry
description: The registry of the fixture modules and its tests, for the SMF pipeline. Not a package to use.
version: 0.0.0
publish_to: none

environment:
  sdk: ">=3.6.0 <4.0.0"

resolution: workspace

dependencies:
  fake_di: any
  fake_feature: any
  fake_infra: any
  fake_roles: any
  fake_router: any
  fake_scaffold: any
  fake_state: any
  smf_contracts: ^0.2.0

dev_dependencies:
  dart_style: ^3.1.0
  file: ^7.0.1
  pub_semver: ^2.2.0
  smf_pipeline: ^0.2.0
  test: ^1.24.0
  very_good_analysis: ^10.3.0
  xml: ^7.0.0
  yaml: ^3.1.2
