/// The module of the main navigation of the app as tabs in a bar at the
/// bottom: `BottomTabsModule`, a provider of the layout role of the SMF
/// module model.
library;

export 'src/bottom_tabs_module.dart';
